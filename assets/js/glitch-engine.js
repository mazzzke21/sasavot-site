/* ============================================
   GLITCH-ENGINE.JS — SAS_ARCHIVE.EXE
   Глитч-движок для сайта-музея «Дело SAS»
   Периодические глитчи с RGB-split и смещением
   ============================================ */

const GlitchEngine = {
    // Disabled - no sudden visual effects, no epilepsy triggers
    // Static VHS textures only (handled by CSS)
    
    init() {
        // Do nothing - glitches are disabled
        console.log('Glitch engine disabled — safe mode');
    },

    trigger() {
        // Do nothing
    },

    triggerHeavy() {
        // Do nothing
    },
    
    triggerRapid() {
        // Do nothing
    }
};